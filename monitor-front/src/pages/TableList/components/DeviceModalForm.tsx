import React from 'react';

export type DeviceModalFormConfig = {
  visible: boolean;
}

const DeviceModalForm: React.FC<DeviceModalFormConfig> = (props) => {
  return <span>modalform</span>;
}

export default DeviceModalForm;

